#include "main.h"

/**
 * _abs - returns absolute value of an int
 *
 * @n: integer from which to get an absolute value
 *
 * Return: 0 (Success)
 */

int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}

	else 
		return (-n);
}
